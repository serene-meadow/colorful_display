# You can check what attributes are are being applied to a file like this:
# `git check-attr --all <path_to_file>`

# These files are generated by Emscripten,
# so don't include them in GitHub language stats.
website/compiled/**/* linguist-vendored
